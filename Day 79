SELECT 
    P.FIRSTNAME, P.LASTNAME,
    A.CITY, A.STATE -- IFNULL() IS NOT NEEDED -> When a LEFT JOIN doesn't find a match, SQL automatically returns: NULL
FROM PERSON AS P 
LEFT JOIN ADDRESS AS A ON P.PERSONID = A.PERSONID;
